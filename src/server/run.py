import threading

def main():
    # 환자 시간 갖고 오기 
    # 시간 확인 
    # 약먹을 환자가 있으면 
    # 주행 시작 
    # 얼굴 확인 
    # 환자인지 체크 음성 & 비전 
    # 약 제조(?) 
    # 약 내리기 
    # 다음 환자 or 제자리 이동 
    """
    while True:
        patient = get_patient_info()
        if patient == None:
            go_home()
            continue
        drive()
        face = face_detection()
        if check_patinet(patient):
            give_drug()
    """
    return 


if __name__ == "__main__":
    main()
